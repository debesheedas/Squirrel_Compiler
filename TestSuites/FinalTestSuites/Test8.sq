`` Check if a list is a palindrome ``
void @start() {
    int arr[5] = {1, 2, 1, 2, 1};

    for(int i = 0; i < 5 / 2; i = i + 1) {
        if (arr[i] != arr[5 - i - 1]) {
            output("No");
            output("\n");
            return;
        }
    }
    output("Yes");
    output("\n");
}