import intMax from <math.acorn>
import intMin from <math.acorn>
replace small -3
replace large -2

void @start(){
    int r = intMax(small, large);
    output("The maximum is ");
    output(r);
    output("\n");

    int arr[2][1][3] = {1,2,3,4,5,6};
    ``
    arr[0][0][0] is 1
    arr[1][0][2] is 6
    ``
    r = intMin(arr[0][0][0], arr[1][0][2]);
    output("The minimum is ");
    output(r);
    output("\n");
}